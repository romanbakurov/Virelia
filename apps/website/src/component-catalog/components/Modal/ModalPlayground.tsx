// Generated by scripts/generators/create-component-page.ts.
// Do not edit manually.

'use client';

import type { ReactNode } from 'react';

import { ComponentPlayground } from '../../shared/ComponentPlayground';
import { useComponentDemoState } from '../../shared/ComponentDemoStateProvider';
import { PlaygroundControlsFromSchema } from '../../shared/PlaygroundControls';

import { modalPlaygroundControls } from './modalPlaygroundSchema';

export type ModalPlaygroundValue = {
  open: boolean;
  closeOnOutsidePress: boolean;
  closeOnEscape: boolean;
  animation: 'scale' | 'slide' | 'fade' | 'none';
  easing:
    'standard' | 'linear' | 'ease' | 'ease-in' | 'ease-out' | 'ease-in-out';
  restoreFocus: boolean;
};

type ModalPlaygroundProps = {
  renderModal: (
    value: ModalPlaygroundValue,
    onChange: <K extends keyof ModalPlaygroundValue>(
      key: K,
      nextValue: ModalPlaygroundValue[K]
    ) => void
  ) => ReactNode;
};

export const initialModalPlaygroundValue: ModalPlaygroundValue = {
  open: false,
  closeOnOutsidePress: false,
  closeOnEscape: false,
  animation: 'scale',
  easing: 'standard',
  restoreFocus: false,
};

export function ModalPlayground({ renderModal }: ModalPlaygroundProps) {
  const [value, setValue] = useComponentDemoState<ModalPlaygroundValue>(
    initialModalPlaygroundValue
  );

  const update = (
    key: keyof ModalPlaygroundValue,
    nextValue: ModalPlaygroundValue[keyof ModalPlaygroundValue]
  ) => {
    setValue({
      ...value,
      [key]: nextValue,
    });
  };

  return (
    <ComponentPlayground
      previewWidth='auto'
      controls={
        <PlaygroundControlsFromSchema
          value={value}
          controls={modalPlaygroundControls}
          onChange={update}
        />
      }
    >
      {renderModal(value, update)}
    </ComponentPlayground>
  );
}
