// Generated by scripts/generators/create-component-page.ts.
// Do not edit manually.

'use client';

import { Modal } from '@vellira-ui/react';

import { ModalPlayground } from './ModalPlayground';

export function ModalDemo() {
  return (
    <ModalPlayground
      renderModal={(value, onChange) => (
        <Modal
          open={value.open}
          onOpenChange={(nextValue) => onChange('open', nextValue)}
          closeOnOutsidePress={value.closeOnOutsidePress}
          closeOnEscape={value.closeOnEscape}
          animation={value.animation}
          easing={value.easing}
          restoreFocus={value.restoreFocus}
        >
          <Modal.Trigger>Open modal</Modal.Trigger>
          <Modal.Overlay />
          <Modal.Content>
            <Modal.Header>
              <Modal.Title>Confirm action</Modal.Title>
              <Modal.Description>
                This action can be reviewed before continuing.
              </Modal.Description>
            </Modal.Header>
            <Modal.Body>Modal body content.</Modal.Body>
            <Modal.Footer>
              <Modal.Close aria-label='Cancel' />
            </Modal.Footer>
          </Modal.Content>
        </Modal>
      )}
    />
  );
}
