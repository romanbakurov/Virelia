// Generated by scripts/generators/component-page/create-component-page.ts.
// Do not edit manually.

'use client';

import type { ComponentPlatform } from '../../types';

import { ComponentCodeBlock } from '../../shared/ComponentCodeBlock';
import { useComponentDemoState } from '../../shared/ComponentDemoStateProvider';

import {
  initialRadioGroupPlaygroundValue,
  type RadioGroupPlaygroundValue,
} from './RadioGroupPlayground';

import styles from '../../shared/ComponentUsage.module.css';

type RadioGroupUsageProps = {
  platform: ComponentPlatform;
};

function createRadioGroupCode(
  platform: ComponentPlatform,
  value: RadioGroupPlaygroundValue
) {
  const packageName =
    platform === 'react' ? '@vellira-ui/react' : '@vellira-ui/react-native';

  const props: string[] =
    platform === 'react'
      ? [`label='Country'`, `description='Choose your country of residence.'`]
      : [`label='Country'`, `description='Choose your country of residence.'`];

  const children =
    platform === 'react'
      ? `  <RadioGroup.Item value='fr' label='France' />
  <RadioGroup.Item value='es' label='Spain' />
  <RadioGroup.Item value='de' label='Germany' />`
      : `  <RadioGroup.Item value='starter' label='Starter' />
  <RadioGroup.Item value='pro' label='Pro' />
  <RadioGroup.Item value='enterprise' label='Enterprise' />`;

  if (value.value) {
    props.push(`value='${value.value}'`);
  }

  if (value.disabled) {
    props.push('disabled');
  }

  if (value.required) {
    props.push('required');
  }

  if (value.orientation !== 'vertical') {
    props.push(`orientation='${value.orientation}'`);
  }

  if (value.size !== 'md') {
    props.push(`size='${value.size}'`);
  }

  if (value.color !== 'primary') {
    props.push(`color='${value.color}'`);
  }

  const propsText = props.length === 0 ? '' : `\n  ${props.join('\n  ')}\n`;

  if (!children) {
    return `import { RadioGroup } from '${packageName}';
${platform === 'react' ? `` : ``}

<RadioGroup${propsText}/>`;
  }

  return `import { RadioGroup } from '${packageName}';
${platform === 'react' ? `` : ``}

<RadioGroup${propsText}>
${children}
</RadioGroup>`;
}

export function RadioGroupUsage({ platform }: RadioGroupUsageProps) {
  const [value] = useComponentDemoState<RadioGroupPlaygroundValue>(
    initialRadioGroupPlaygroundValue
  );

  const code = createRadioGroupCode(platform, value);

  return (
    <section className={styles.root}>
      <div className={styles.header}>
        <h2 className={styles.title}>Usage</h2>

        <p className={styles.description}>
          Import the component and configure it with the same props used in the
          playground.
        </p>
      </div>

      <ComponentCodeBlock code={code} />
    </section>
  );
}
