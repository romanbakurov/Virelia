export type ModalOverlayProps = {
  /** Enables enter and exit animation for the overlay. */
  animated?: boolean;
  /** Class name applied to the overlay element. */
  className?: string;
  /** Keeps the overlay mounted even when the modal is closed. */
  forceMount?: boolean;
};
