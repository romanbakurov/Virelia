import type { ReactNode } from 'react';

export type PortalProps = {
  children: ReactNode;
  container?: unknown;
};

export type PortalProviderProps = {
  children: ReactNode;
  container?: unknown;
};
