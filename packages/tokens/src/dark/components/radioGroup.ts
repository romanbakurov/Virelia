import { createRadioGroupTokens } from '../../factories/createRadioGroupTokens.js';
import { spacing } from '../../tokens/spacing.js';

export const radioGroup = createRadioGroupTokens({
  spacing1: spacing[1],
  spacing2: spacing[2],
  spacing3: spacing[3],
  spacing4: spacing[4],
  spacing5: spacing[5],
});
